import { FormatarHoraCorrigidaPipe } from './formatar-hora-corrigida.pipe';

describe('FormatarHoraCorrigidaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatarHoraCorrigidaPipe();
    expect(pipe).toBeTruthy();
  });
});
